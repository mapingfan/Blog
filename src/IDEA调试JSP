# IDEA中调试JSP页面

最近在用`IDEA`写`jsp`页面,每次页面上稍微改动一点，就得重启服务器，实在是过于繁琐。后来我想`IDEA`不至于这么傻，上网一搜，果然有好的解决方案。

下面给出了解决方案的地址。其中比较核心的就是配置服务器时，选择当页面没有被选中时自动更新类和资源。这样就可以简单的刷新`JSP`页面而不用频繁重启服务器。

`IDEA`中调试`jsp`文件技巧，无需频繁重启服务器。参见[调试][1]

2017年11月3日 留

[1]: http://blog.csdn.net/lxfHaHaHa/article/details/68926080
