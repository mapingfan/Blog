# JSP中include指令与动作的对比

学习jsp时候会遇到一个十分有趣的现象，比如我们想在jsp页面中包含一个文件，我们有如下做法：一是使用`<%@ include file...`，二是使用
`<jsp:include page`。这两个有什么区别？
首先，上面的是运行之前就编译好，下面是执行时遇到才编译。
其次下面这个可以传递参数，上面不行。
最后就是，上面的相当于完全拷贝插入，执行时依赖插入的页面。下面的页面有自己的逻辑，依赖于自己。